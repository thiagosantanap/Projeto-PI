<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_fim</item>
        <item name="colorPrimaryDark">@color/color_fim</item>
        <item name="colorAccent">@color/color_fim</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/color_fim</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/color_fim</item>
        <item name="colorControlNormal">@color/color_fim</item>
        <item name="colorControlActivated">@color/color_fim</item>
    </style>
</resources>
